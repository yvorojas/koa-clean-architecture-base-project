module.exports = class {

    constructor(constants, environment, packagVars) {
        this.constants = constants;
        this.environment = environment;
        this.packagVars = packagVars;
    }
  
  };